version: '3.4'

services:
  ecommercemicroservice:
    image: ecommercemicroservice
    build:
      context: .
      dockerfile: ./dockerfile
    environment:
      NODE_ENV: production
    ports:
      - 3000:3000
